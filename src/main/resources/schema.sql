CREATE TABLE quantity_measurement (
                                      id INT AUTO_INCREMENT PRIMARY KEY,
                                      value DOUBLE,
                                      unit VARCHAR(50),
                                      operation VARCHAR(50),
                                      result DOUBLE
);